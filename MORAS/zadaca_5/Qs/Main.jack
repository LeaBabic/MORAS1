class Main{
 function void main() {
  var Array data;
  var int idx;
  let idx = 0;
  let data = Array.new(4);
  let data[0] = 1;
  let data[1] = 2;
  let data[2] = 1;
  let data[3] = 5;

  while (idx < 4) {
   do Output.printInt(data[idx]);
   let idx = idx + 1;
  }
  
  do Output.println();
  do Qs.qsort(data, 0, 3);
  let idx = 0;

  while (idx < 4) {
   do Output.printInt(data[idx]);
   let idx = idx + 1;
  }

  do data.dispose();
  return;
 }
}
